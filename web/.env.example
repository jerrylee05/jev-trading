# Trader dry-run API (no trailing slash). The desk opens EventSource on /events.
# Run the trader with PORT=3010. The desk itself listens on 3001.
NEXT_PUBLIC_API_URL=http://127.0.0.1:3010

# Multi-desk API (Phase 3+)
NEXT_PUBLIC_DESK_URL=http://127.0.0.1:3020
